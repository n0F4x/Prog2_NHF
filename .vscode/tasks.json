{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build the project",
            "type": "shell",
            "command": "./build.sh",
            "args": [
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ]
}